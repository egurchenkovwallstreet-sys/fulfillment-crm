# Аудит реализации vs ТЗ

**Дата:** 24.08.2026  
**Статус:** Документы сверены с кодом (`main` = `9e7a590` + этот коммит)  
**Вывод:** складской цикл FBS закрыт. Следующий модуль по ТЗ — **§11 Возвраты**.

---

## Что уже работает в коде (не только в старых заметках)

| Модуль ТЗ | Код | Где в UI |
|-----------|-----|----------|
| §5 Приёмка | ✅ | `/intake`, `/cells` |
| Хаб склада (каталог WB, онбординг, перенос FBS) | ✅ сверх базового §5 | `/warehouse` |
| §6–§8 Сборка, ЧЗ, счётчики = ЛК WB | ✅ `delivery-v11` | `/assembly` |
| §9 Поставки + QR | ✅ в том же окне сборки | `/assembly/:id` |
| §10 Списание остатков | ✅ при «В доставку» и sync `done` | автоматически |
| §7 Печать Xprinter | ✅ агент `.exe` | `/print-agent` |
| §12–§13 Тарифы, кабинет, `/billing` | 🟡 базово готово | `/sellers`, `/cabinet`, `/billing` |
| §11 Возвраты | ❌ нет | — |
| §4 UI токена WB | ❌ только Django-admin | `/admin/` |

---

## Расхождения, которые были в журнале

В `PROGRESS.md` шапка ещё писала «следующий шаг — §9, §10, Xprinter», а таблица сверки помечала их как несделанные. В коде это уже лежит:

- `2700640` / `9e7a590` — UI поставок, блокировки шагов, списание
- `89448ae` / страница `/print-agent` — агент печати
- `34d7c2a` — модуль «Склад»

Журнал приведён к факту.

---

## Очередь (для новичка: один шаг за раз)

1. **Сейчас:** задеплоить текущий `main` на сервер (команда ниже).
2. **Следующая разработка:** §11 Возвраты (кнопка как приёмка, та же ячейка, флаг «возврат»).
3. Потом: поле токена WB в разделе «Селлеры» (чтобы не лезть в Django-admin).
4. Потом: финансы — отчёты за день/месяц/всё время и задолженность селлера.

---

## Деплой

```bash
cd /opt/fulfillment-crm && git pull && bash scripts/deploy.sh
```

Проверка: дашборд http://5.129.243.246:8080  
Версия синка: `GET /api/health/` → `"sync_version":"delivery-v11"`

---

**Конец документа**
